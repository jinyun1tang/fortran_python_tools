       program main

C this is an example f77 file
C

      if (xxx)then
      if(xxx) then
      if (xx)call sth

      if(xx  .and. xx
     2xlkdgag
     3xlklgg)then
      endif
      if (xbg)then
      else
      endif

      if a then
      elseif b
     2then
      elseif c then
      ELSEIF(IDAY(7,NB,NZ,NY,NX).EQ.0)THEN

      endif
      endif
      endif
      end program main
